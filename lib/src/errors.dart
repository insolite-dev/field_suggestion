// All expected errors.
class Errors {
  static const String searchByORcustomSearchIsNull =
      "If given suggestionList's runtimeType isn't List<String>, List<int> or List<double>. That means, you was gave a List which includes classes. So then [searchBy] can't be null. Or otherwise [customSearch] must be implemented ";
}
