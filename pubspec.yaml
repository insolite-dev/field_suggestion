name: field_suggestion
description: FieldSuggestion help us to make beautiful type ahead fields, By listening values of given TextEditingController and checking if the given value is in the list or not. 
version: 0.1.0
author:
homepage: https://github.com/theiskaa/field_suggestion

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
